version: '2'

services:

    pub.restapi:
        image: arcgis-restapi-utrecht:0.1.0
        restart: always
        expose:
            - "8080"
        ports:
            - "${IP_ADDR}8080:8080"
        environment:
            - DB_USER=publisher
            - DB_PASSWORD=publisher